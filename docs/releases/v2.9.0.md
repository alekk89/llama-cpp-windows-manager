- **Copy Profile To Another Model**: resizable dialog with close button, model dropdown shows name + path, and a context-menu entry on saved profiles in the Models grid.
- **Copy Profile dialog polish**: field labels (Model, New Profile Name, Runtime), runtime dropdown uses searchable combo with favorites/stars (same as profile card), "Missing runtime" pseudo-entry filtered out, description text corrected.
- Added a **Copy Profile To Another Model** button in launch-profile settings to duplicate a saved profile onto a different model.
- Show saved launch-profile favorites first, with their star state, in the **Settings → Load profiles on startup** picker.
