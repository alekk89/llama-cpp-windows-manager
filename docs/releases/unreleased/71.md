- Show separate local and LAN gateway endpoints and make clear that local health
  does not verify access from another device.
- Detect the Manager's scoped Windows Firewall rule and provide explicit,
  reversible install and remove actions for the current gateway port.
