- Reattach a relocated GGUF from its missing model row or through `llwmctl` while preserving launch profiles, groups, favorites, startup selections, and model history.
- Consolidate an already-imported duplicate after checking active sessions and the stored GGUF identity.
